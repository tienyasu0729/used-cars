# Example only.
# The application reads runtime configuration from .env, not from this file.
#
# Keep real local values in .env.
# Add new VITE_* keys to .env first, then document them here only if needed.
